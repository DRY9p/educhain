<div class="max-w-2xl bg-emerald-100 rounded-lg p-1.5">
  <h1 class="text-xl font-bold my-5">Messages</h1>

  <!-- OMG finnaly i found it :) after hour of researching -->
  <%= turbo_stream_from :messages %>
  
  <div id="messages" class="max-w-2xl">
    <%= render MessageComponent.with_collection(@messages)%> 
  </div>

  <%= render "shared/form"%>
</div>